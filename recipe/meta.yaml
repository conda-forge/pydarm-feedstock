{% set name = "pydarm" %}
{% set version = "0.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pydarm-{{ version }}.tar.gz
  sha256: 45b4f2b3f1e842210f56ecdd2f49794ea7351202a7cb7d21f9e8e352872c7c5a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools-scm
  run:
    - corner
    - dttxml
    - emcee
    - gwpy
    - h5py
    - matplotlib-base
    - numpy
    - python >=3.7
    - requests
    - scikit-learn <=1.0.2
    - scipy

test:
  imports:
    - pydarm
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/pydarm/
  summary: pyDARM modeling package for LIGO detector calibration
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - eagoetz
